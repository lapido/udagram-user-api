apiVersion: v1
kind: Service
metadata:
  name: udagram-user-api
  labels:
    service: udagram-user-api
spec:
  ports:
  - name: "8080"
    port: 8080
    targetPort: 8080
  selector:
    service: udagram-user-api